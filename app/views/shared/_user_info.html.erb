<a href="<%= user path(current user) %>"
    <%= gravatar_for current_user, size: 52 %>
</a>
<h1><%= current_user.name %></h1>
<span><%= link_to "view my profile", current_user %></span>
<span><%= pluralize(current user.microposts.count, "micropost") %></span>
